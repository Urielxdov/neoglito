model User {
  id               Int               @id @default(autoincrement())
  userId           Int               @unique
  email            String            @unique
  createdAt        DateTime          @default(now())
  githubConnection GitHubConnection?
}
