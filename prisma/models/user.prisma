model User {
  id               Int               @id @default(autoincrement())
  githubConnectionId Int?            @unique
  email            String            @unique
  createdAt        DateTime          @default(now())
  githubConnection GitHubConnection? @relation(fields: [githubConnectionId], references: [id])
}
